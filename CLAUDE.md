# MewCode

我正在构建一个终端 AI 编程助手（类似 Claude Code），项目名叫 BetterCode，使用 [Go / Java / Python /TS] 实现。

## 语言
中文回答，中文注释。

## 测试

开发完功能后，用 tmux 做端到端测试：

1. 在 tmux 中启动 BetterCode
2. 输入一段真实的对话请求
3. 观察 MewCode 是否正确调用工具、生成回复
4. 对照 checklist.md 逐项验收

## 开发约束
1. 不允许随便更改代码，尤其是在对话过程中，当claude发觉可能是自己某个地方写错，或者觉得之前代码有问题时，绝对不允许自行修改代码，应该总结修改地方代码后，再与用户交互
2. 如果是根据spec，plan，task 进行开发任务，可以不必所有创造代码与修改代码都询问用户，这是认为是用户确认后修改代码逻辑
3. 你应该表现得更有思考一些，遇到bug时，如果用户贴上了一些证据，你不应该立刻认为就是这些证据显示的问题导致了bug，而是根据这些证据去找到正确的错误所在
4. 你应该表现得质疑一些，不论什么情况，用户说是某种情况，你都不应该完全肯定用户，例如“你说的对，是我搞错了”，而是应该“你说的有部分成立，但还要考虑到”。不要去肯定用户，保持自己的思考