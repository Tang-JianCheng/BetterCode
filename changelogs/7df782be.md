# 7df782be

- **提交时间**：2026-07-31 10:21:00
- **提交类型**：feat
- **提交分支**：main

## 修改摘要

为 BetterCode 增加声明式 Hook 系统：支持用户、项目共享、项目本地三层 YAML 严格加载，以系统、会话、用户任务、消息和工具十个生命周期事件触发自动化规则，并通过公共匹配器提供精确、glob、正则和反向条件。新增 Shell、Prompt、HTTP 和子 Agent 占位四类动作，支持进程内一次执行、后台任务、超时、取消、脱敏日志和下一次 Provider 请求的单次 Prompt 注入；`pre_tool_use` 可用统一结构化协议拒绝工具，同时保持 Plan Mode、权限系统和 Agent 恢复能力。完整 Spec、Plan、Tasks、Checklist 与 README 同步落库，全量 258 项自动化测试通过。

## 影响文件

| 文件路径 | 变更类型 | 说明 |
|----------|----------|------|
| `docs/hook-system/*.md` | 新增 | 记录 Hook 系统需求、技术设计、任务拆解和完整验收证据 |
| `src/matcher/pattern.ts` | 新增 | 提供权限和 Hook 共用的精确、glob、正则匹配基础能力 |
| `src/hook/types.ts`、`src/hook/field.ts`、`src/hook/template.ts` | 新增 | 定义 Hook 契约、事件字段访问和结构化模板渲染 |
| `src/hook/config-loader.ts`、`src/hook/compiler.ts` | 新增 | 实现三层 YAML、环境变量展开和启动期严格组合校验 |
| `src/hook/command-executor.ts`、`src/hook/http-executor.ts` | 新增 | 实现有界 Shell 与 HTTP 动作、超时、取消和资源清理 |
| `src/hook/action-executor.ts`、`src/hook/logger.ts` | 新增 | 统一动作与 allow/deny 协议，写入脱敏有界 JSONL 日志 |
| `src/hook/manager.ts` | 新增 | 管理生命周期、条件分发、once、后台任务和 Prompt 队列 |
| `src/hook/*.test.ts`、`src/matcher/pattern.test.ts` | 新增 | 覆盖真实子进程、本地 HTTP、生命周期、拦截和 Prompt 消费 |
| `src/agent/loop.ts`、`src/agent/tool-scheduler.ts` | 修改 | 发布消息与工具事件，在最终工具结果后执行 Hook 并回灌拒绝 |
| `src/chat/manager.ts`、`src/skill/runner.ts` | 修改 | 接入会话、轮次、用户消息和独立 Skill 生命周期 |
| `src/prompt/reminder.ts`、`src/prompt/types.ts` | 修改 | 把 Hook Prompt 注入下一次运行时补充指令并一次性消费 |
| `src/permission/rule-parser.ts` | 修改 | 迁移到公共 matcher，同时保持权限配置外部行为兼容 |
| `src/index.tsx`、`src/tool/types.ts` | 修改 | 装配 Hook 运行时、系统生命周期和 `HOOK_DENIED` 错误码 |
| `.gitignore`、`README.md` | 修改 | 忽略本地配置与日志，记录事件、动作协议和信任边界 |
